<Window x:Class="SuppManagerDB.WPF.Windows.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Авторизація" 
        Height="320" Width="480"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#e6ffff">

    <Grid Margin="40">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
            <RowDefinition Height="Auto" MinHeight="46.117" />
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <TextBlock Text="Вхід у систему"
                   Grid.Row="0"
                   FontSize="24"
                   Foreground="#003366"
                   FontWeight="Bold"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,5" Width="170"/>

        <!-- Поля -->
        <StackPanel Grid.Row="1" VerticalAlignment="Center" >

            <TextBlock Text="Логін" FontSize="14" Foreground="#444" Margin="0 17 0 4"/>
            <TextBox x:Name="txtLogin" Height="32" FontSize="15" Padding="5" />

            <TextBlock Text="Пароль" FontSize="14" Foreground="#444" Margin="0 8 0 4"/>
            <PasswordBox x:Name="txtPassword" Height="32" FontSize="15" Padding="5"/>

            <TextBlock x:Name="txtError" Foreground="Red" FontSize="13" Text=""
                       Margin="0 6 0 0" TextWrapping="Wrap"/>

        </StackPanel>

        <!-- Кнопка -->
        <Button Grid.Row="2"
                Content="Увійти"
                Width="120"
                Background="#003366"
                Foreground="White"
                FontWeight="SemiBold"
                HorizontalAlignment="Center"
                Margin="0,18,0,0"
                Click="Login_Click"/>

    </Grid>

</Window>
